<div>
  <h2>Users</h2>
  <ul>
    <% for user in @users %>
        <% if user.id != current_user.id  %>
            <li>
              <%=h user.email %>
               <%= link_to "Add Friend", friendships_path(:friend_id => user), :method => :post %>
            </li>
        <% end %>
    <% end %>
  </ul>
</div>